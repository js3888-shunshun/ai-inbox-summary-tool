# dependencies
node_modules/

# build output
dist/

# secrets — never commit
.env
.env.local
*.local

# sqlite data
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
data/

# logs / os
*.log
.DS_Store

# local demo recordings — too large for GitHub, keep out of the repo
*.mp4
*.mov
*.mkv
*.zip


# challenge brief (keep local, not in repo)
Coding Challenge.pdf
